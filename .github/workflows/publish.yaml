# https://github.com/vitejs/vite/actions/runs/11026340522/workflow
name: Publish Package

on:
  push:
    tags:
      - "v*" # Push events to matching v*, i.e. v1.0, v20.15.10

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publishing ${{ github.ref }}"